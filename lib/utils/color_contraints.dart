import 'package:flutter/material.dart';

class ColorConstraints {
  // Color primaryColor = Colors.orange;
  // Color primaryColor = Color.fromARGB(255, 124, 118, 110);
  // Color primaryColor = Color(0xff000000);

  static Color primaryColor = Color(0xfff57224);
  static Color textColorDark = Color(0xff000000);
}
